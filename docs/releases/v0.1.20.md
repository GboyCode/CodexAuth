# CodexAuth Switch 0.1.20

- 导入改为先选文件、再输入密码，支持多选和 Ctrl+A 全选，最多 100 个凭证文件。
- 同一批导出的账号共用一次密码输入；错误密码可直接重试，不必重新选文件。
- 整批解密校验通过后才写入；不同密码或损坏的文件明确提示重新选择，不静默部分导入。
- 已有账号统一确认更新，当前已登录账号保留本机凭证；重复账号去重，写入失败恢复原凭证。

已验证批量导入、密码重试、混合密码、取消选择、重复更新确认、写入回滚和界面交互。
