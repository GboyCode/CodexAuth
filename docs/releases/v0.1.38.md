# CodexAuth Switch v0.1.38

- 修复 Codex 26.924 桌面本地接口新增必填字段后，自动恢复无法读取任务状态的问题；所有任务接口请求统一传入 `callerSource`。
- 自动切换倒计时默认显示在额度浮窗左侧靠下；空间不足时自动调整到可见区域。
- 除取消按钮外，倒计时卡片均可拖动；位置保存在本机设置中，下次弹出和重启工具后继续使用。屏幕断开或分辨率改变时，将位置限制在当前可用屏幕内。
- 保留原有 15 秒倒计时、取消本次、Esc 取消和任务恢复检查。

## English

Fixes automatic recovery with Codex 26.924 by supplying the required `callerSource` on desktop tool calls. The countdown card now defaults to the lower side of the quota widget, supports dragging outside the cancel button, and remembers its position across openings and app restarts. Saved positions are clamped to an available display. Countdown cancellation and recovery checks are preserved.
