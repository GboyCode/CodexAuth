# CodexAuth Switch 0.1.17

- 导出入口更名为“导出账号凭证”，点击弹窗选择“导出当前”或“导出全部”。
- 全部导出在所选位置创建新文件夹，每个账号保存为独立的加密 `.codexauth` 文件，共用本次迁移密码，兼容现有导入功能。
- 包含全部已保存账号及当前登录账号；当前账号优先使用最新登录凭证，避免重复导出。
- 凭证读取失败时明确提示，不静默跳过账号；写入失败清理本次新建的未完成导出文件夹。

验证覆盖单账号与全部导出、加密往返与导入兼容、当前凭证更新、取消操作、未登录/空账号/损坏凭证，以及弹窗选择和密码校验。
