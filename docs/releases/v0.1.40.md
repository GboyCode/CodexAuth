# CodexAuth Switch v0.1.40

- 将新导入、缺少重置时间或超过七天未更新的额度记录作为未核验候选，不直接判定没有额度。
- 优先尝试有有效记录的 Plus、有 5 小时限制的 Business、仅周额度的 Business，以及其他有记录账号。之后按 Plus、Business、其他套餐的顺序尝试额度未知账号。
- 同类有记录候选仍按瓶颈剩余额度由高到低、重置时间由近到远排序。
- 仅在切换后确认账号身份匹配且实际额度可用时继续任务；确认耗尽后保留排除记录，未知候选也不会因新一轮恢复或重启工具被重复尝试。
- 剩余不超过 2% 且未重置、需要重新登录或无法核验身份的未知账号继续跳过。
- 保留 v0.1.39 的耗尽记忆，以及 v0.1.38 的桌面接口修复和倒计时位置记忆。

## English

Newly added accounts and accounts with missing reset times or stale quota snapshots can now serve as unverified fallback candidates. Recorded candidates retain their existing priority; unknown candidates are tried afterward in Plus, Business, then other-plan order. Continuation still requires matching live account identity and available quota. Confirmed exhaustion persists across recovery rounds and restarts, including for accounts with unknown reset times.
